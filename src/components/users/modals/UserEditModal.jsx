'use client'
import { useState, useEffect } from "react"
import { db } from "@/lib/firebaseConfig"
import { useUsers } from "@/context/UsersContext"
export default function UserEditModal({ open, onClose, user, onUpdated }) {
    return teste
}